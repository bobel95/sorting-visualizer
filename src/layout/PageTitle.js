import React from 'react';
import "./PageTitle.css";

const PageTitle = () => {
    return (
        <div id="page-title">
            <h2>Sorting Visualizer</h2>
        </div>
    );
}

export default PageTitle;